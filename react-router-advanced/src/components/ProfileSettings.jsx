import React from 'react';


const ProfileSettings = () => {
    return (
      <div>
        <h2>Profile Details</h2>
        <p>This is the settings page needed to config the profile.</p>
      </div>
    );
  };
export default ProfileSettings;  